// Placeholder — À remplacer par votre worker.js complet
export default {
  async fetch(request, env, ctx) {
    return new Response('WECRAFT Worker — À configurer', {
      status: 200,
      headers: { 'Content-Type': 'text/plain' },
    });
  },
};
